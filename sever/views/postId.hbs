<!DOCTYPE html>
<html lang="en">

{{> baseHead customTitle = customTitle}}

<body>
    {{> siteHeader}}
        <div class="main">  
    {{#if post}}
    <div class="bodyPost">
    <div class="headPost box">
        <h2>{{post.title}}</h2>
        <p>
            <span>
                {{post.form}}
            </span>
            <span>
                {{post.createdAt}}
            </span>
        </p>

        <p>
            {{
            post.content
            }}
        </p>
    </div>
    

    <hr>
    <form action="/p/{{post.id}}/comment" method="post">

        <p>
            <textarea name="content" id="" class="inText" rows="3"placeholder="แสดงความคิดเห็น">{{value.content}}</textarea>
        </p>
        <p>
            <input type="text" name="from" class="inText"placeholder="จากคุณ" value="{{value.from}}">
        </p>

        <p>
            <input type="checkbox" name="accepted" id="input-accepted">
            <label for="input-accepted">ข้อความยาวๆที่หลายคนเคยเจอ อาจไม่ต้องอ่านก็ได้ แค่ยอมรับและเข้าใจก็พอ</label>
        </p>
        <p>
            <button type="submit" class="btn">คอมเม้นท์</button>
        </p>

        {{#if errormessage}}
        <p>
            {{errormessage}}
        </p>
        {{/if}}
    </form>
    <hr>
    {{#if postComments}}
    <h4>{{postComments.length}} คอมเม้นท์</h4>
    {{else}}
    <h3>ยังไม่มีคอมเม้นท์นะ มาเปิดกันได้</h3>
    {{/if}}
    {{#each postComments}}

    <div class="box" >

        <p>
            <span>
                {{this.from}}
            </span>
            <span>
                {{this.createdAt}}
            </span>
        </p>
        <p>
            {{this.content}}
        </p>
    </div>

    {{/each}}
    </div>
    {{else}}
    <p>Not found</p>
    {{/if}}
    </div>

     {{> button}}
          <script src="../../config/top.js"></script>
    {{> siteFooter}}
</body>

</html>